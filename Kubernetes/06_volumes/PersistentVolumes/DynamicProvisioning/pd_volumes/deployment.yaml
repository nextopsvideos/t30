apiVersion: apps/v1
kind: Deployment  
metadata:
  name: dyn-deployment
spec:
  replicas: 10
  selector:
    matchLabels:
      app: my-app
  template:
    metadata:
      labels:
        app: my-app
    spec:
      containers:
        - name: dyn-container
          image: nginx:latest
          ports:
            - containerPort: 80
          volumeMounts:
          - name: dyn-disk-vol 
            mountPath: /dynvoldata
      volumes:
      - name: dyn-disk-vol 
        persistentVolumeClaim:
          claimName: dyn-disk-vol-claim