apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-httpd.fullname" . }}-test-connection"
  labels:
    {{- include "my-httpd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-httpd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
