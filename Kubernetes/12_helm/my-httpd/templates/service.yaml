apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-httpd.fullname" . }}
  labels:
    {{- include "my-httpd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-httpd.selectorLabels" . | nindent 4 }}
