apiVersion: v1
kind: Service
metadata:
  name: {{ include "my_calc.fullname" . }}
  labels:
    {{- include "my_calc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my_calc.selectorLabels" . | nindent 4 }}
